#include <bits/stdc++.h>

using namespace std;
using ll = long long;


void uwu(){
    // Reminder about how to use vectors
    vector<ll> x = {2, 3, 5};

    for (int i = 0; i< x.size(); i++){
        cout << x.at(i) << endl;
    }
}

signed main(){
    cin.tie(0) -> sync_with_stdio(0);
    int t = 1;
    cin>>t;
    while(t--){
        uwu();
    }
}
